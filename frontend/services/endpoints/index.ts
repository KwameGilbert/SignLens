/**
 * Barrel export for all API endpoint modules.
 *
 * Usage:
 *   import { authEndpoints, predictEndpoints } from "@/services/endpoints";
 */

export * as authEndpoints from "./auth";
export * as predictEndpoints from "./predict";
export * as historyEndpoints from "./history";
export * as userEndpoints from "./user";
