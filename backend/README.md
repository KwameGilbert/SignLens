# SignLens Mobile App Backend Gateway

This is the gateway backend service for the SignLens mobile application, built using **Python and FastAPI**. It acts as a secure intermediary between the mobile client and the heavy machine learning prediction endpoints.

## 🚀 Key Responsibilities

1. **User Management & Authentication**: User registration, login, and secure token issuance using JSON Web Tokens (JWT) and bcrypt hashing.
2. **Translation History Tracking**: Saving and retrieving users' sign language practice logs and translation history.
3. **ML Prediction Proxy**: Proxying static image upload predictions to the machine learning backend model server using `httpx`.
4. **Real-time WebSocket Relay**: Proxying WebSocket streams between the mobile app and the MediaPipe/LSTM ML model prediction server, performing JWT verification on handshake.

---

## 📂 Directory Structure

```text
backend/
├── app/
│   ├── api/                 # API endpoint routers
│   │   ├── api_v1/          # Version 1 endpoints (auth, predict, history)
│   │   └── deps.py          # Dependency injection (e.g. get_current_user)
│   ├── core/                # Core helpers (database session, JWT security)
│   ├── models/              # SQLAlchemy Database Models (User, History)
│   ├── schemas/             # Pydantic validation schemas (User, History, Predict)
│   ├── config.py            # Settings loader & environment variables
│   └── main.py              # Application entry point
├── requirements.txt         # Package dependencies
├── .env.example             # Environment configuration template
└── README.md                # This documentation
```

---

## 🛠️ Setup & Installation

### 1. Create a Virtual Environment (Python 3.10)
Ensure Python 3.10 is installed on your machine. You can create a new virtual environment:

```powershell
# In the signlens/backend directory
py -3.10 -m venv .venv
.\.venv\Scripts\activate
```

### 2. Install Dependencies
```powershell
pip install -r requirements.txt
```

### 3. Configuration
Copy the `.env.example` file to `.env`:

```powershell
copy .env.example .env
```

Open `.env` and configure:
* `SECRET_KEY`: Set to a secure random hex string for JWT signing.
* `MODEL_API_KEY`: Paste the API key generated by the model endpoints database (e.g., using `generate_api_key.py` in `model/model_endpoints`).
* `MODEL_API_URL` / `MODEL_API_WS_URL`: The URLs where the model endpoints are running (defaults to `http://127.0.0.1:8000`).

---

## 🏃 Running the Server

Start the FastAPI application using `uvicorn`:

```powershell
uvicorn app.main:app --reload --port 8001
```

Once running:
* **Interactive Docs (Swagger UI)**: Open [http://127.0.0.1:8001/docs](http://127.0.0.1:8001/docs) in your browser.
* **Redoc View**: Open [http://127.0.0.1:8001/redoc](http://127.0.0.1:8001/redoc).

---

## 🔐 API Reference

### 1. Authentication (`/api/v1/auth`)
* `POST /api/v1/auth/register`: Register a new user profile.
* `POST /api/v1/auth/login`: Authenticate email + password and retrieve a JWT access token.
* `GET /api/v1/auth/me`: Retrieve the authenticated user's profile details.

### 2. Predictions Gateway (`/api/v1/predict` & `/api/v1/predict-stream`)
* **`POST /api/v1/predict` (HTTP)**: Proxies static image files (`UploadFile`) to the ML model server, logs the prediction in the user's history database, and returns the prediction result.
* **`GET /api/v1/predict-stream` (WebSocket)**: Relays real-time video frames.
  * Connect to: `ws://127.0.0.1:8001/api/v1/predict-stream?token=<JWT_TOKEN>&type=<stream|video>`
  * Once authenticated, send raw video frame bytes (JPEG/PNG).
  * The server will respond with JSON predictions in real-time. High-confidence predictions will be saved to the database.

### 3. Translation History (`/api/v1/history`)
* `GET /api/v1/history/`: Retrieve paginated translation logs for the authenticated user.
* `POST /api/v1/history/`: Manually write a translation log to the database.
